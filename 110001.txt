110001

a 110
b 11
c 0
d 001

check(pos, str, codeLet)

check(0, "", legend[0] {
    char temp[]
    strcpy(temp, str)

    if (strncmp() )
        strcat(temp, "a") -> "a"
        if (pos == end) { 
            possibilities++
            addPossibility();
        }
        for
            check(3, "a", {legend})
}


